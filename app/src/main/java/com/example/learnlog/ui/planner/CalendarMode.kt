package com.example.learnlog.ui.planner

import com.example.learnlog.data.model.CalendarMode as ModelCalendarMode

// Alias to maintain compatibility while using the canonical version from data.model
typealias CalendarMode = ModelCalendarMode
